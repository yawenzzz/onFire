[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "polymarket-arb"
version = "0.1.0"
description = "Fail-closed structural arbitrage research scaffold"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["polymarket_arb"]
